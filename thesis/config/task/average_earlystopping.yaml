name: average_earlystopping

number_of_runs: 10

path_to_save: ./thesis/data/avgs_early_stopping

#method 
training_params:
  learning_rate: 0.001
  epochs: 100
  batch_size: 100
  weight_decay: 0.01
  
  use_contrastive_loss: False
  use_cross_entropy_weighting: False
  save_model: False
  early_stopping: True
  patience: 5

use_pretrained: False